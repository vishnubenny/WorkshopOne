<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.vishnu.workshopone.fraghome.HomeFragmentViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white">

        <Button
            android:id="@+id/btnUserProfile"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/colorPrimary"
            android:onClick="@{() -> viewModel.onShowFragment2Clicked()}"
            android:text="@string/go_to_user_profile"
            android:textAllCaps="false"
            app:layout_constraintBottom_toBottomOf="parent" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/dimen_8dp"
            android:background="?attr/colorPrimary"
            android:onClick="@{() -> viewModel.tapOnComposeHomeActivity()}"
            android:text="@string/navigate_to_compose_home_activity"
            android:textAllCaps="false"
            app:layout_constraintBottom_toTopOf="@id/btnUserProfile" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
